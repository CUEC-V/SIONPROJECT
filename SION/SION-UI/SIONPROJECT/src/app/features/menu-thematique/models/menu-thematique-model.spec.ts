import { MenuThematiqueModel } from './menu-thematique-model';

describe('MenuThematiqueModel', () => {
  it('should create an instance', () => {
    expect(new MenuThematiqueModel()).toBeTruthy();
  });
});
