import { ReseauSocialModel } from './reseau-social-model';

describe('ReseauSocialModel', () => {
  it('should create an instance', () => {
    expect(new ReseauSocialModel()).toBeTruthy();
  });
});
